import {theme as base} from '@chakra-ui/react'
// placed name as it is on Google fonts

const fonts = {
    fonts: {
        heading: `"Plus Jakarta Sans", ${base.fonts?.heading}, sans-serif`,
        body: `"Montserrat", ${base.fonts?.body}, sans-serif`
    }
}

export default fonts
